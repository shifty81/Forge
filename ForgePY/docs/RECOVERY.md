# Vault Recovery

Every universal patch transaction stores preimage evidence beneath the Vault Library recovery tree before mutating governed source. Failed writes are rolled back in reverse order. Applied transports remain archived separately from project source so recovery evidence survives repository cleanup.

Project-native patch authorities remain supported when they are stronger than the universal fallback; their successful receipts are reconciled into the same Vault Library history.
