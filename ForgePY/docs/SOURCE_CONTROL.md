# Vault Source Control Authority

Vault treats Git as mandatory project infrastructure and tracks GitHub plus the Cortex-owned/local Forgejo surface. This milestone retains source-control discovery/status while the next authority pass will absorb GREEN-gated commit/push and Forgejo lifecycle operations into Vault itself rather than delegating those writes to project-specific controllers.
