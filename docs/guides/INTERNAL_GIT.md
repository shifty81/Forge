# Internal Git (legacy name)

This name is retained only for compatibility with older ForgePY settings and repositories. The canonical subsystem is **ForgeGit**. See `FORGEGIT.md`. Existing `InternalGit` repositories and `forgepy-internal` remotes are adopted rather than discarded.
